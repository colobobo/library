export enum Status {
  active = 'active',
  arrived = 'arrived',
  dropped = 'dropped',
  waiting = 'waiting',
}

export enum Skins {
  slobbery = 'slobbery',
  lifeguard = 'lifeguard',
  injured = 'injured',
  rebel = 'rebel',
  glasses = 'glasses',
}
