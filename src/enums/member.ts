export enum Status {
  active = 'active',
  arrived = 'arrived',
  dropped = 'dropped',
  waiting = 'waiting',
}

export enum Skins {
  fatty = 'fatty',
  scout = 'scout',
  injured = 'injured',
  rebel = 'rebel',
  intellectual = 'intellectual',
}
