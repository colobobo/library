export enum EventsTransition {
  init = 'transition:init',
  playerReady = 'transition:player:ready',
  start = 'transition:start',
  next = 'transition:next',
  nextSuccess = 'transition:next:success',
  ended = 'transition:ended',
}
